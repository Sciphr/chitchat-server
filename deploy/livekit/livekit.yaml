port: 7880
bind_addresses:
  - "0.0.0.0"
rtc:
  port_range_start: 50000
  port_range_end: 50100
  tcp_port: 7881
  use_external_ip: true
# TURN is configured at runtime via the LIVEKIT_TURN_DOMAIN environment variable.
# If LIVEKIT_TURN_DOMAIN is set, the startup script will inject the TURN block below.
# Do not add a turn: section here manually.
logging:
  level: info
